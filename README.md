# Deep Device Reports

Upload this entire folder to a Node.js-capable hosting account.

Run:
npm install
npm start

The home page collects browser/server-visible diagnostics and saves every run
as a separate JSON file inside /reports.

The Reports table shows:
- serial number
- date & time
- filename
- Open link

Each report can be opened on report.html.

No GPS/location permission is requested.
